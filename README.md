
To install project
```
pipenv install --dev
```


To run project => /auth
```
python3 manage.py runserver   
```



Install new packages with pipenv

```
pipenv install pytest-django --dev    

```

Run test with 

```
pipenv run pytest -s  

```